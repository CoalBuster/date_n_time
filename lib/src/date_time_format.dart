import 'package:intl/intl.dart';

class DateTimeFormat {
  /// The ISO date formatter that formats or parses a date without an offset, such as '2024-10-22'.
  static final ISO_LOCAL_DATE = DateFormat('yyyy-MM-dd');
}
